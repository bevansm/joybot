version: '3.2'
services:
  joybot:
    build: .
    env_file:
      - .env
    ports:
      - '80:80'
    volumes:
      - type: bind
        source: ./local-db
        target: /local-db
    container_name: joybot

