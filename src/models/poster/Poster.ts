import { IGame } from './../data-types';
import IPoster, { IPostOptions } from './IPoster';

const Poster: IPoster = {
  post: async (game: IGame, options: IPostOptions) => {
    // TODO: Implement this
  },
};

export default Poster;
